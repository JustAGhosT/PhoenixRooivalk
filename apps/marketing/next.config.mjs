/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  reactStrictMode: true,
  // No authentication - public marketing site
  // Build timestamp: 2025-10-08
};

export default nextConfig;
